{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "id": "#child",
  "properties": {
    "something": {
      "type": "string"
    },
    "recursive": { "$ref": "http://localhost:8081/recursive/schema.json" }
  },
  "required": ["something", "recursive"]
}
